// Configuración del frontend
// Re-exporta la configuración principal para facilitar el acceso

export { default as config } from '../../config/index.js';
export { default as servicesConfig } from '../../config/services.js';
export { default as themeConfig } from '../../config/theme.js';

// También puedes importar directamente:
// import config from '@/config';
// import servicesConfig from '@/config/services';
// import themeConfig from '@/config/theme';
